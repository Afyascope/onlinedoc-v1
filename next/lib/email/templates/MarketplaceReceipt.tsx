import { BaseTemplate, EmailButton, EmailHeading, EmailText } from "./BaseTemplate";
export type ReceiptItem = { name: string; quantity: number; price: string };
export function MarketplaceReceipt({ name, orderId, items, total, currency, libraryUrl }: { name?: string; orderId: string; items: ReceiptItem[]; total: string; currency: string; libraryUrl: string }) { return <BaseTemplate title="Your OnlineDoc receipt"><EmailHeading>Purchase confirmed</EmailHeading><EmailText>Hi {name || "there"}, thank you for your purchase. Order {orderId} is ready.</EmailText><table role="presentation" width="100%" style={{ borderCollapse: "collapse", margin: "20px 0" }}><tbody>{items.map((item) => <tr key={item.name}><td style={{ padding: "8px 0", borderBottom: "1px solid #E2E8F0" }}>{item.name} × {item.quantity}</td><td align="right" style={{ padding: "8px 0", borderBottom: "1px solid #E2E8F0" }}>{currency} {item.price}</td></tr>)}<tr><td style={{ paddingTop: 12, fontWeight: 700 }}>Total</td><td align="right" style={{ paddingTop: 12, fontWeight: 700 }}>{currency} {total}</td></tr></tbody></table><EmailButton href={libraryUrl}>Open download library</EmailButton></BaseTemplate>; }
