import { BaseTemplate, EmailButton, EmailHeading, EmailText } from "./BaseTemplate";
export function ConsultationBooked({ recipientName, title, consultationUrl, role = "patient" }: { recipientName?: string; title: string; consultationUrl: string; role?: "patient" | "clinician" }) { return <BaseTemplate title="Consultation update"><EmailHeading>{role === "clinician" ? "New consultation request" : "Consultation booked"}</EmailHeading><EmailText>Hi {recipientName || "there"}, {role === "clinician" ? `a new consultation, “${title}”, is ready for your attention.` : `your consultation, “${title}”, has been booked successfully.`}</EmailText><EmailButton href={consultationUrl}>View consultation</EmailButton></BaseTemplate>; }
