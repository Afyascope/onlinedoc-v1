import { BaseTemplate, EmailButton, EmailHeading, EmailText } from "./BaseTemplate";
export function ClinicianApproved({ name, dashboardUrl, approved = true, reason }: { name?: string; dashboardUrl: string; approved?: boolean; reason?: string }) { return <BaseTemplate title={approved ? "OnlineDoc account approved" : "OnlineDoc account update"}><EmailHeading>{approved ? "Your account has been approved" : "Your clinician application update"}</EmailHeading><EmailText>Hi {name || "there"}, {approved ? "Congratulations — your clinician account has been approved." : `your clinician application was not approved at this time.${reason ? ` Reason: ${reason}` : ""}`}</EmailText><EmailButton href={dashboardUrl}>{approved ? "Open dashboard" : "Contact support"}</EmailButton></BaseTemplate>; }
